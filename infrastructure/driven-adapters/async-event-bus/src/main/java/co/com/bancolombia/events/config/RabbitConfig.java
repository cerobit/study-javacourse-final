package co.com.bancolombia.events.config;


public class RabbitConfig {
    /*
    @Bean
    public Queue boxQueue() {
        return new Queue("ScaffoldQuickStart.subsEvents", true);
    }

    @Bean
    public TopicExchange domainEventsExchange() {
        return new TopicExchange("domainEvents");
    }

    @Bean
    public Binding binding(Queue boxQueue, TopicExchange domainEventsExchange) {
        return BindingBuilder.bind(boxQueue).to(domainEventsExchange).with("box.event.created");
    }

     */
}